using System;
using System.Collections.Generic;

namespace Gradebook
{
    class Program
    {
        static void Main()
        {
            Console.WriteLine("What's the name of the student?");
            string name = Console.ReadLine();
            Console.WriteLine("Enter the the students grades seprated by a comma or 'ok' to move to the next student");
            int grade = Console.ReadLine();
        }
        public void Mean()
        {
            
        }
    }
}
